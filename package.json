{
  "name": "animenu",
  "version": "1.0.0",
  "description": "Animenu is a responsive dropdown navigation.",
  "scripts": {
    "autoprefixer": "postcss -u autoprefixer -r css/*",
    "scss": "node-sass -o css/ sass/ --output-style compressed",
    "build": "npm run scss && npm run autoprefixer"
  },
  "dependencies": {
    "autoprefixer": "^8.6.3",
    "node-sass": "^4.9.0",
    "postcss-cli": "^5.0.1"
  }
}
